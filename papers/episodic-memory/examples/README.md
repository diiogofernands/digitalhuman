# Examples

This directory contains optional example entry points.

- `synthetic_pipeline.py`: end-to-end synthetic experiment using the ReSuME package
- `synthetic_config.yaml`: default configuration for the example pipeline

These files are not required to use the core package in `resume/`.
